import NewsDetails from "../pages/NewsDetails";

const appRoutes = [{ path: "/:slug", element: NewsDetails }];

const userRoutes = [];

const noUserRoutes = [];

export { appRoutes, userRoutes, noUserRoutes };
